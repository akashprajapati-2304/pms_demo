# Standalone Dockerfile for obfuscated self-contained bundle
FROM node:18-alpine

WORKDIR /app

# Copy the pre-built obfuscated standalone bundle
COPY dist/server.standalone.obf.js ./

# Expose port
EXPOSE 5000

# Start the application
CMD ["node", "server.standalone.obf.js"]
