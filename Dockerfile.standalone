# Standalone Dockerfile for self-contained bundle
FROM node:18-alpine

WORKDIR /app

# Copy the pre-built standalone bundle (no dependencies needed)
COPY dist/server.standalone.js ./

# Expose port
EXPOSE 5000

# Start the application
CMD ["node", "server.standalone.js"]
